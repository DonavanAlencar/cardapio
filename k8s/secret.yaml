apiVersion: v1
kind: Secret
metadata:
  name: cardapio-db-secret
  namespace: cardapio
type: Opaque
data:
  # Usando a secret mysql-root-password existente
  # O usuário será root e a senha virá da secret mysql-root-password
  DB_USER: cm9vdA==  # root
  # A senha será referenciada da secret mysql-root-password existente 